+++
title = "API-Fence"
description = "Envoy HTTP filter for OpenAPI-based request and response validation. Written in Rust."
weight = 3

[extra]
url = "https://github.com/proxyconf/api-fence"
repo = "https://github.com/proxyconf/api-fence"
language = "Rust"
+++

API-Fence is an Envoy HTTP filter written in Rust that validates requests and responses against OpenAPI specifications at the proxy layer. It enforces your API contract before traffic reaches the backend — catching schema violations, unexpected parameters, and malformed payloads at the edge.
